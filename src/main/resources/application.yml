server:
  port: 8080

spring:
  application:
    name: secure-chat-backend

  datasource:
    url: jdbc:postgresql://${PGHOST:crossover.proxy.rlwy.net}:${PGPORT:38497}/${PGDATABASE:railway}?sslmode=require
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:TanaaPuWURXTAUwTMoqSOiMmxfWvSmCm}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

app:
  file-storage:
    upload-dir: uploads
